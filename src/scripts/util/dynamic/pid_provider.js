/** Entirely used to get a PID to use for a port handler that isn't actually associated with a process since this immediately exits */

/** @param {import("@ns").NS} ns */
export async function main(ns) {
}